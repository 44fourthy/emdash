/**
 * The page script is produced at build time by the
 * `emdash:visual-inspector-page-script` Vite plugin (see
 * tooling/vite/visual-inspector-page-script.ts) and inlined as a string.
 */
declare module 'virtual:emdash-visual-inspector-page-script' {
  const pageScript: string;
  export default pageScript;
}
